spring:
  application:
    name: learning-assistant-workflow

learning-assistant:
  qwen:
    api-key: ${DASHSCOPE_API_KEY:}
    base-url: ${DASHSCOPE_BASE_URL:https://dashscope.aliyuncs.com/compatible-mode/v1}
    model-name: ${DASHSCOPE_MODEL_NAME:qwen-plus}
    temperature: ${DASHSCOPE_TEMPERATURE:0.2}
    max-tokens: ${DASHSCOPE_MAX_TOKENS:4096}
