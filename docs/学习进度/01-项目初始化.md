# 01 - 项目初始化

这一节只做一件事：把 Spring Boot 工程先搭起来。

## 目标

- IDEA 能直接导入
- `mvn spring-boot:run` 能启动
- 预留后续加入 LangGraph4j / LangChain4j 的位置

## 本节完成的内容

- Spring Boot 3.5.16
- Java 21
- 最小应用入口
- `docs/学习进度` 和 `docs/版本记录`

## 验证

- 工程可以被 Maven 编译
- 应用可以启动
- 控制台会打印初始化信息

